---
phase: quick-260525-j03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_25_000001_add_latency_ms_to_diagnostic_results_table.php
  - app/DTOs/ProviderResult.php
  - app/Services/PrismStructuredCaller.php
  - app/Repositories/DiagnosticResultRepository.php
  - app/Models/DiagnosticResult.php
autonomous: true
requirements: [QUICK-260525-j03]

must_haves:
  truths:
    - "Every persisted DiagnosticResult row carries a latency_ms integer measured around the provider call"
    - "PrismStructuredCaller::call() measures wall-clock time around (new DiagnosticAgent)->prompt() and passes it through to the DTO"
    - "Existing fromPrismResponse() call sites in tests still compile without modification"
  artifacts:
    - path: "database/migrations/2026_05_25_000001_add_latency_ms_to_diagnostic_results_table.php"
      provides: "latency_ms integer column on diagnostic_results"
      contains: "latency_ms"
    - path: "app/DTOs/ProviderResult.php"
      provides: "latencyMs readonly int property + fromPrismResponse latencyMs param"
      contains: "latencyMs"
    - path: "app/Services/PrismStructuredCaller.php"
      provides: "hrtime measurement around prompt() call"
      contains: "hrtime"
    - path: "app/Repositories/DiagnosticResultRepository.php"
      provides: "latency_ms persisted in create()"
      contains: "latency_ms"
    - path: "app/Models/DiagnosticResult.php"
      provides: "latency_ms in fillable + integer cast"
      contains: "latency_ms"
  key_links:
    - from: "app/Services/PrismStructuredCaller.php"
      to: "app/DTOs/ProviderResult.php"
      via: "latencyMs passed to fromPrismResponse()"
      pattern: "latencyMs:"
    - from: "app/Repositories/DiagnosticResultRepository.php"
      to: "diagnostic_results.latency_ms"
      via: "create() array key"
      pattern: "'latency_ms'\\s*=>\\s*\\$dto->latencyMs"
---

<objective>
Add `latency_ms` tracking to each LLM provider call so the research corpus records how long each provider took to respond.

Purpose: Latency is a key comparison axis for the TCC multi-LLM study. Capturing it per-call makes provider performance reproducible alongside diagnostic quality.
Output: A new migration, an extended `ProviderResult` DTO, wall-clock timing in `PrismStructuredCaller`, and persistence wiring through the repository + model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Current contracts the executor extends. Use these directly — no codebase exploration needed. -->

app/DTOs/ProviderResult.php — private constructor + static factory.
Properties use camelCase (DB columns use snake_case): tokensInput -> tokens_input.
The factory `fromPrismResponse()` currently takes:
  (StructuredAgentResponse $response, string $provider, string $model, string $requestId, string $promptVersion): self
StructuredAgentResponse does NOT contain timing — latency is measured by the caller and passed in.

app/Services/PrismStructuredCaller.php — call(string $code, string $statement, string $requestId): ProviderResult
  Calls: $response = (new DiagnosticAgent)->prompt($userMessage, provider:, model:)
  Then:  ProviderResult::fromPrismResponse(response:, provider:, model:, requestId:, promptVersion:)
  Then:  $this->repository->save($result)

app/Repositories/DiagnosticResultRepository.php — save(ProviderResult $dto): DiagnosticResult
  Maps camelCase DTO props to snake_case columns in DiagnosticResult::create([...]).

app/Models/DiagnosticResult.php — Eloquent model with $fillable (snake_case) and casts() method.
  Existing integer casts: 'tokens_input' => 'integer', 'tokens_output' => 'integer'.

Migration pattern (from 2026_05_22_000001_add_error_code...):
  return new class extends Migration { up() { Schema::table('diagnostic_results', fn(Blueprint $table) => ...) } down() {...dropColumn...} };
  NOTE: latency_ms needs NO CHECK constraint and NO rawColumn — it is a plain integer.

IMPORTANT — backward compatibility:
  fromPrismResponse() is called from 4 test files (ProviderResultTest, DiagnosticResultPersistenceTest,
  PrismStructuredCallerTest indirectly, DiagnoseControllerTest via $set reflection helper). To avoid breaking
  those call sites, the new `latencyMs` parameter on fromPrismResponse() MUST be optional with a default of `0`.
  The DTO constructor property `latencyMs` should likewise default to `0` so reflection-built test stubs stay valid.
  Production timing flows through PrismStructuredCaller, which DOES pass the measured value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add latency_ms column migration + model wiring</name>
  <files>database/migrations/2026_05_25_000001_add_latency_ms_to_diagnostic_results_table.php, app/Models/DiagnosticResult.php</files>
  <action>
    Create the migration `database/migrations/2026_05_25_000001_add_latency_ms_to_diagnostic_results_table.php`
    following the anonymous-class pattern of `2026_05_22_000001_add_error_code_to_diagnostic_results_table.php`,
    but WITHOUT rawColumn/CHECK (latency_ms is a plain integer):
      - up(): Schema::table('diagnostic_results', function (Blueprint $table) { $table->integer('latency_ms')->default(0); });
      - down(): Schema::table('diagnostic_results', function (Blueprint $table) { $table->dropColumn('latency_ms'); });
      - Use `declare(strict_types=1);` and the same use-statements (Migration, Blueprint, Schema).
      - default(0) is required so the ALTER succeeds against existing rows (SQLite needs a default when adding NOT NULL to a populated table — same constraint noted in the error_code migration).

    In `app/Models/DiagnosticResult.php`:
      - Add `'latency_ms'` to the `$fillable` array (place it after `'tokens_output'` to mirror DTO/column grouping).
      - Add `'latency_ms' => 'integer'` to the array returned by `casts()` (alongside the existing tokens integer casts).
  </action>
  <verify>
    <automated>php artisan migrate:fresh --env=testing 2>&1 | tail -5 && php artisan tinker --execute="echo Schema::hasColumn('diagnostic_results','latency_ms') ? 'HAS_COLUMN' : 'MISSING';"</automated>
  </verify>
  <done>Migration creates an integer latency_ms column (default 0); model $fillable and casts include latency_ms as integer; migrate:fresh succeeds and Schema::hasColumn reports HAS_COLUMN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add latencyMs to ProviderResult DTO + measure timing in PrismStructuredCaller + persist in repository</name>
  <files>app/DTOs/ProviderResult.php, app/Services/PrismStructuredCaller.php, app/Repositories/DiagnosticResultRepository.php</files>
  <behavior>
    - PrismStructuredCallerTest: a successful call() persists a row whose latency_ms is an integer >= 0 (assert column present and integer-typed on reload).
    - ProviderResult DTO: fromPrismResponse() called WITHOUT latencyMs (existing test call sites) still constructs a valid DTO with latencyMs === 0 — no signature break.
    - ProviderResult DTO: fromPrismResponse() called WITH latencyMs: 123 yields $dto->latencyMs === 123.
  </behavior>
  <action>
    In `app/DTOs/ProviderResult.php`:
      - Add a readonly property to the private constructor: `public readonly int $latencyMs = 0,` (place it after `$tokensOutput`; default 0 keeps reflection-built test stubs and existing factory calls valid).
      - Add an optional parameter to `fromPrismResponse()`: `int $latencyMs = 0,` (place it after `$promptVersion`). Pass it through in the `new self(...)` call as `latencyMs: $latencyMs,`.
      - Do NOT read latency from $response — StructuredAgentResponse has no timing field. It comes only from the parameter.

    In `app/Services/PrismStructuredCaller.php`:
      - Measure wall-clock time around the `(new DiagnosticAgent)->prompt(...)` call using hrtime for monotonic precision:
          $startedAt = hrtime(true);
          $response = (new DiagnosticAgent)->prompt(...);  // existing call, unchanged args
          $latencyMs = (int) round((hrtime(true) - $startedAt) / 1_000_000);
      - Pass `latencyMs: $latencyMs,` into the `ProviderResult::fromPrismResponse(...)` call (add it after `promptVersion:`).
      - Do not change the call() signature or the repository->save() invocation.

    In `app/Repositories/DiagnosticResultRepository.php`:
      - Add `'latency_ms' => $dto->latencyMs,` to the `DiagnosticResult::create([...])` array (place it after `'tokens_output'`).
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Unit/DTOs/ProviderResultTest.php tests/Feature/Ai/PrismStructuredCallerTest.php tests/Feature/Persistence/DiagnosticResultPersistenceTest.php 2>&1 | tail -20</automated>
  </verify>
  <done>fromPrismResponse() accepts an optional latencyMs (default 0) without breaking existing call sites; PrismStructuredCaller measures hrtime around prompt() and passes the integer ms through; repository persists latency_ms; the three named test files pass.</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --env=testing` succeeds; `diagnostic_results` has an integer `latency_ms` column.
- DTO `ProviderResult` exposes a readonly `int $latencyMs` and `fromPrismResponse()` accepts it optionally (default 0) — no existing call site breaks.
- `PrismStructuredCaller::call()` wraps the `prompt()` call in `hrtime` measurement and forwards the millisecond value.
- Repository `save()` writes `latency_ms`; a persisted row round-trips an integer latency value.
- Full suite green: `./vendor/bin/pest 2>&1 | tail -10`.
</verification>

<success_criteria>
- A row persisted via the production path (`PrismStructuredCaller::call()`) has a non-negative integer `latency_ms` reflecting wall-clock time around the provider call.
- All existing tests still pass (no signature regressions in `fromPrismResponse()`).
- Migration is reversible (`down()` drops `latency_ms`).
</success_criteria>

<output>
After completion, create `.planning/quick/260525-j03-add-latency-ms-tracking-to-each-llm-prov/260525-j03-SUMMARY.md`
</output>
